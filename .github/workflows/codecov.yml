name: Upload coverage reports to Codecov
on:
    pull_request:
        branches: [main, develop]
jobs: 
    coverage:
        runs-on: ubuntu-latest
        steps:
            - uses: codecov/codecov-action@2.311.0
              env:
                  CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
