name: Upload coverage reports to Codecov
on:
    pull_request:
        branches: [main, develop]
jobs: 
    coverage:
        runs-on: ubuntu-latest
        steps:
            - uses: codecov/codecov-action@0.1.15
              env:
                  CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
